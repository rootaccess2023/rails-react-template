<!DOCTYPE html>
<html>
  <head>
    <title>Your App Name</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav style="padding: 1rem; background: #f8f9fa; border-bottom: 1px solid #dee2e6;">
      <%= link_to "Home", root_path, style: "margin-right: 1rem;" %>
      
      <% if user_signed_in? %>
        Welcome, <%= current_user.full_name %>!
        <%= link_to "Edit Profile", edit_user_registration_path, style: "margin-left: 1rem;" %>
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, style: "margin-left: 1rem;" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, style: "margin-right: 1rem;" %>
        <%= link_to "Sign Up", new_user_registration_path %>
      <% end %>
    </nav>

    <main style="padding: 2rem;">
      <% if notice %>
        <div style="color: green; margin-bottom: 1rem;"><%= notice %></div>
      <% end %>
      
      <% if alert %>
        <div style="color: red; margin-bottom: 1rem;"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>